// https://nextjs.org/docs/basic-features/layouts#single-shared-layout-with-custom-app

import Head from 'next/head';

import styles from '../styles/Layout.module.scss';

export default function Layout({ children, backgroundColor }: { children: JSX.Element | JSX.Element[]; backgroundColor?: string }) {
  return (
    <div className={styles.container} style={{ backgroundColor }}>
      <Head>
        <title>paint_color_gallery using colornerd</title>
        {/* <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" /> */}
      </Head>
      <main className={styles.main}>{children}</main>
      <footer className={styles.footer} />
    </div>
  );
}
